const symbol = {};

let index = 0;

const Sym = (key) => {
  if (symbol[key])
    return symbol[key];

  symbol[key] = key;
  return symbol[key];

};


const range = (st, end, step = 1) => {
return {


  
}
};